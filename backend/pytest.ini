[pytest]
# Collect the maintained suite, not executable helpers such as _smoke_test.py.
testpaths = tests
norecursedirs = scripts
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Disable auto-loading of external plugins (e.g., langsmith) to avoid import errors
addopts = -p no:langsmith
